// @ts-ignore
import { Icon } from 'components';

fetch('something').then(() => {});

type variant = string;

export default (variant?: variant, filled?: boolean) => {
  fetch('something').then(() => {});
  return Icon;
};
